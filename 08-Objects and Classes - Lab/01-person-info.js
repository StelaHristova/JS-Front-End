function solve(firstName, lastName, age) {
    const personInfo = {
        firstName: firstName,
        lastName: lastName,
        age: age,
    };

    return personInfo;
}

solve("Peter", 
"Pan",
"20");